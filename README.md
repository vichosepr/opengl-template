Plantilla de OpenGL(c++) para vscode en Linux
- OpenGL 3.3 Core
- GLFW
- GLAD
- g++
- VS Code (tasks.json)

Antes de usar ejecutar en consola el comando:

```bash
sudo apt install libglfw3 libglfw3-dev mesa-common-dev
